<MudDialog>
    <DialogContent>
        <MudText>@Context.ImageUrl</MudText>
        <MudText>@Context.Text</MudText>
        <MudText>@Context.Author</MudText>
        <MudText>@Context.EmbededCode</MudText>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Отменить</MudButton>
        <MudButton Color="Color.Secondary" Variant="Variant.Filled" OnClick="Delete">Удалить</MudButton>
    </DialogActions>
</MudDialog>

@code { 
    
    [CascadingParameter] MudDialogInstance MudDialog { get; set; }

    [Parameter] public Picture Context { get; set; }

    void Delete() => MudDialog.Close(DialogResult.Ok(Context));
    void Cancel() => MudDialog.Cancel(); 
}
